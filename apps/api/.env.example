# Database Configuration
# Para Supabase, use DATABASE_URL com a connection string completa
# Formato: postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/postgres?sslmode=require
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.kazqtrmkqoztachcphlt.supabase.co:5432/postgres?sslmode=require

# OU use variáveis individuais (para desenvolvimento local)
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=minc_teams
# DATABASE_PASSWORD=password
# DATABASE_NAME=minc_teams

# SSL Configuration (opcional, geralmente necessário para Supabase)
DATABASE_SSL=true

# Node Environment
NODE_ENV=development

# Server
PORT=3001

# JWT
JWT_SECRET=your-secret-key-here
JWT_EXPIRATION=86400

# Refresh Token
REFRESH_TOKEN_EXPIRATION_DAYS=7

# Password Reset
RESET_PASSWORD_TOKEN_EXPIRATION_MINUTES=60

# CORS
FRONTEND_URL=http://localhost:5173,http://localhost:3000
CORS_ORIGIN=http://localhost:5173,http://localhost:3000
